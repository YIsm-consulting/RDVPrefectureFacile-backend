[phases.setup]
nixPkgs = [
  "nodejs_20",
  "chromium",
  "nss",
  "freetype",
  "freetype.dev",
  "harfbuzz",
  "ca-certificates",
  "ttf-dejavu"
]

[phases.build]
cmds = ["npm install"]

[start]
cmd = "node server.js"
